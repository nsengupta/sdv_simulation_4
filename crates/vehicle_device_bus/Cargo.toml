[package]
name = "vehicle_device_bus"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
socketcan = "3.5.0"

[dev-dependencies]
tokio = { version = "=1.48.0", features = ["macros", "rt-multi-thread"] }
